---
"@plaisolutions/client": minor
"@plaisolutions/react": minor
---

Add session-aware audio transcription to `PlaiThreadTransport`, `PlaiChat`,
and `useChat`, and require `SpeechToTextToggle` consumers to provide a
transcription function explicitly.
